#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int num;
printf("Enter number: "); 
    scanf("%d",&num);
    int list[num];
    printf("Enter %d elements: ",num);
    for(int i=0;i<num;i++){
        scanf("%d",&list[i]);
    }
    int value,found=0;
    printf("Enter value that you have to search:");
    scanf("%d",&value);
    for(int i=0;i<num;i++){
        if(value==list[i]){
            found=1;
        }
    }
    if(found==1){
        printf("Found");
    }
    else if(found==0){
        printf("Not Found");
    }
    return 0;
}
