#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int num;
    scanf("%d",&num);
    int list[num];
    for(int i=0;i<num;i++){
        scanf("%d",&list[i]);
    }
    int value,found=0;
    scanf("%d",&value);
    for(int i=0;i<num;i++){
        if(value==list[i]){
            found=1;
        }
    }
    if(found==1){
        printf("Found");
    }
    else if(found==0){
        printf("Not Found");
    }
    return 0;
}
