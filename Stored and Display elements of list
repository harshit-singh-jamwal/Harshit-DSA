#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int num;
    scanf("%d",&num);
    int list[num];
    for(int i=0;i<num;i++){
        scanf("%d",&list[i]);
    }
    for(int i=0;i<num;i++){
        printf("%d ",list[i]);
    }
    return 0;
}
